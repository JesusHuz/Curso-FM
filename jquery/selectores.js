$(document).ready(function(){
    //$('h1').hide();//.show lo muestra
    //$('h1,h2').hide();

    //con clases
    //$('.encabezado').hide();//el punto es identificar una clase no un elementos

    //com  id
    //$('#primero').hide();//el rpimer parrafo

    //el primer parrafo que tiene el id
    //$('p#primero').hide();

    //enlace dentro de parrafo
    $('.primero a').hide();


});